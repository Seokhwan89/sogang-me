{"name": "Unpack source zip", "on": {"push": {"paths": ["sogang-me.zip"]}}, "permissions": {"contents": "write"}, "jobs": {"unpack": {"runs-on": "ubuntu-latest", "steps": [{"uses": "actions/checkout@v4"}, {"run": "unzip -o sogang-me.zip -d _tmp && cp -r _tmp/sogang-me/. . && rm -rf _tmp sogang-me.zip && git config user.name unpack-bot && git config user.email bot@users.noreply.github.com && git add -A && (git commit -m 'Unpack source' || echo nothing) && git push"}]}}}
