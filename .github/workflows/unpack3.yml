{"name": "Unpack zip v3", "on": {"push": {"paths": ["*.zip"]}, "workflow_dispatch": null}, "permissions": {"contents": "write"}, "jobs": {"unpack": {"runs-on": "ubuntu-latest", "steps": [{"uses": "actions/checkout@v4"}, {"run": "z=$(ls *.zip | head -1); if [ -z \"$z\" ]; then echo no zip; exit 0; fi; unzip -o \"$z\" -d _tmp && rm -rf _tmp/sogang-me/.github && cp -r _tmp/sogang-me/. . && rm -rf _tmp *.zip && git config user.name unpack-bot && git config user.email bot@users.noreply.github.com && git add -A && (git commit -m 'Unpack source v3' || echo nothing) && git push"}]}}}
